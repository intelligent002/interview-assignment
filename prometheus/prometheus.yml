global:
  scrape_interval: 60s

scrape_configs:
  - job_name: prometheus
    static_configs:
    - targets:
      - localhost:9090

  - job_name: 'sub'
    dns_sd_configs:
      - names:
          - 'sub'
        type: 'A'
        port: 8080

rule_files:
  - "/etc/prometheus/rules/*.yml"  # Define your alerting rules here
